name: World Cup Calendar Sync

on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  PUBLIC_CALENDAR_ID: ${{ secrets.PUBLIC_CALENDAR_ID }}
  PORTUGAL_CALENDAR_ID: ${{ secrets.PORTUGAL_CALENDAR_ID }}
  NORWAY_CALENDAR_ID: ${{ secrets.NORWAY_CALENDAR_ID }}
  ARGENTINA_CALENDAR_ID: ${{ secrets.ARGENTINA_CALENDAR_ID }}
  COLOMBIA_CALENDAR_ID: ${{ secrets.COLOMBIA_CALENDAR_ID }}
  CAPEVERDE_CALENDAR_ID: ${{ secrets.CAPEVERDE_CALENDAR_ID }}
  BOSNIA_CALENDAR_ID: ${{ secrets.BOSNIA_CALENDAR_ID }}
  BELGIUM_CALENDAR_ID: ${{ secrets.BELGIUM_CALENDAR_ID }}
  GERMANY_CALENDAR_ID: ${{ secrets.GERMANY_CALENDAR_ID }}

jobs:
  sync-calendar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Restore npm cache
        uses: actions/cache@v5
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm install

      - name: Write Google service account key
        run: |
          printf '%s\n' "${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}" > service-account.json

      - name: Run calendar sync
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
        run: npx tsx main.ts